CONTENTS OF FOLDER

model_training_&_predictions.pd - the patch for training a model from a dataset stored a text file as output by /dataset_collection/PC_side.pd
scale_sensors.pd - a simple scaling abstraction for scaling sensor values, to allow for accounting for different lighting conditions when collecting the dataset

USAGE

- Open model_training_&_predictions.pd
- Change filepaths for the dataset, and saving and loading models as necessary
- Follow the numbered instructions in the patch